<Window x:Class="MedicalLabAnalyzer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedicalLabAnalyzer.Views"
        mc:Ignorable="d"
        Title="MedicalLabAnalyzer - تحليل المختبر الطبي" 
        Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <!-- Navigation Item Style -->
        <Style x:Key="NavigationItemStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ContentPresenter x:Name="ContentPresenter"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Navigation Panel -->
        <Border Grid.Column="0" 
                Background="{DynamicResource MaterialDesignSurface}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0,0,1,0">
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" 
                        Background="{DynamicResource MaterialDesignPrimary}"
                        Padding="20,16">
                    <StackPanel>
                        <TextBlock Text="MedicalLabAnalyzer"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="تحليل المختبر الطبي"
                                   FontSize="14"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Navigation Menu -->
                <ScrollViewer Grid.Row="1" 
                              VerticalScrollBarVisibility="Auto"
                              Margin="16,16,16,0">
                    <StackPanel>
                        <!-- Dashboard -->
                        <RadioButton x:Name="DashboardButton"
                                    Style="{StaticResource NavigationItemStyle}"
                                    GroupName="Navigation"
                                    IsChecked="True"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="Dashboard">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ViewDashboard" 
                                                       Width="20" 
                                                       Height="20" 
                                                       Margin="0,0,12,0"/>
                                <TextBlock Text="Dashboard - لوحة التحكم"/>
                            </StackPanel>
                        </RadioButton>

                        <!-- Patients -->
                        <RadioButton x:Name="PatientsButton"
                                    Style="{StaticResource NavigationItemStyle}"
                                    GroupName="Navigation"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="Patients">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountMultiple" 
                                                       Width="20" 
                                                       Height="20" 
                                                       Margin="0,0,12,0"/>
                                <TextBlock Text="Patients - المرضى"/>
                            </StackPanel>
                        </RadioButton>

                        <!-- Exams -->
                        <RadioButton x:Name="ExamsButton"
                                    Style="{StaticResource NavigationItemStyle}"
                                    GroupName="Navigation"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="Exams">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileDocument" 
                                                       Width="20" 
                                                       Height="20" 
                                                       Margin="0,0,12,0"/>
                                <TextBlock Text="Exams - الفحوصات"/>
                            </StackPanel>
                        </RadioButton>

                        <!-- Video Analysis -->
                        <RadioButton x:Name="VideoAnalysisButton"
                                    Style="{StaticResource NavigationItemStyle}"
                                    GroupName="Navigation"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="VideoAnalysis">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Video" 
                                                       Width="20" 
                                                       Height="20" 
                                                       Margin="0,0,12,0"/>
                                <TextBlock Text="Video Analysis - تحليل الفيديو"/>
                            </StackPanel>
                        </RadioButton>

                        <!-- Reports -->
                        <RadioButton x:Name="ReportsButton"
                                    Style="{StaticResource NavigationItemStyle}"
                                    GroupName="Navigation"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="Reports">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileChart" 
                                                       Width="20" 
                                                       Height="20" 
                                                       Margin="0,0,12,0"/>
                                <TextBlock Text="Reports - التقارير"/>
                            </StackPanel>
                        </RadioButton>

                        <!-- Settings -->
                        <RadioButton x:Name="SettingsButton"
                                    Style="{StaticResource NavigationItemStyle}"
                                    GroupName="Navigation"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="Settings">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cog" 
                                                       Width="20" 
                                                       Height="20" 
                                                       Margin="0,0,12,0"/>
                                <TextBlock Text="Settings - الإعدادات"/>
                            </StackPanel>
                        </RadioButton>

                        <!-- Backup -->
                        <RadioButton x:Name="BackupButton"
                                    Style="{StaticResource NavigationItemStyle}"
                                    GroupName="Navigation"
                                    Command="{Binding NavigateCommand}"
                                    CommandParameter="Backup">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="BackupRestore" 
                                                       Width="20" 
                                                       Height="20" 
                                                       Margin="0,0,12,0"/>
                                <TextBlock Text="Backup - النسخ الاحتياطي"/>
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>
                </ScrollViewer>

                <!-- User Info -->
                <Border Grid.Row="2" 
                        Background="{DynamicResource MaterialDesignSurfaceVariant}"
                        Padding="16"
                        Margin="16,16,16,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <materialDesign:PackIcon Grid.Column="0"
                                               Kind="AccountCircle"
                                               Width="32"
                                               Height="32"
                                               Foreground="{DynamicResource MaterialDesignPrimary}"
                                               Margin="0,0,12,0"/>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding CurrentUser.FullName}"
                                       FontWeight="Medium"
                                       FontSize="14"/>
                            <TextBlock Text="{Binding CurrentUser.Role}"
                                       FontSize="12"
                                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        </StackPanel>

                        <Button Grid.Column="2"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Command="{Binding LogoutCommand}"
                                ToolTip="Logout - تسجيل خروج">
                            <materialDesign:PackIcon Kind="Logout" Width="20" Height="20"/>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource MaterialDesignSurface}"
                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                    BorderThickness="0,0,0,1"
                    Padding="20,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="{Binding CurrentPageTitle}"
                               FontSize="20"
                               FontWeight="Medium"
                               VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center">
                        
                        <!-- Notifications -->
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Command="{Binding ShowNotificationsCommand}"
                                ToolTip="Notifications - الإشعارات"
                                Margin="0,0,8,0">
                            <materialDesign:PackIcon Kind="Bell" Width="20" Height="20"/>
                        </Button>

                        <!-- Help -->
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Command="{Binding ShowHelpCommand}"
                                ToolTip="Help - المساعدة"
                                Margin="0,0,8,0">
                            <materialDesign:PackIcon Kind="HelpCircle" Width="20" Height="20"/>
                        </Button>

                        <!-- About -->
                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Command="{Binding ShowAboutCommand}"
                                ToolTip="About - حول"
                                Margin="0,0,8,0">
                            <materialDesign:PackIcon Kind="Information" Width="20" Height="20"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content Frame -->
            <Frame Grid.Row="1" 
                   x:Name="MainFrame"
                   NavigationUIVisibility="Hidden"
                   Content="{Binding CurrentPage}"/>
        </Grid>
    </Grid>
</Window>