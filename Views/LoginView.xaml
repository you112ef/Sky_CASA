<Window x:Class="MedicalLabAnalyzer.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Medical Lab Analyzer - Login" Height="250" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Medical Lab Analyzer" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <TextBox x:Name="UsernameBox" Placeholder="Username" Margin="0,0,0,10"/>
        <PasswordBox x:Name="PasswordBox" Placeholder="Password" Margin="0,0,0,10"/>
        
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="Login" Click="LoginButton_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>